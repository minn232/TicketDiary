## 6/23(월) ~ 6/29(일) — 크롤링 구현 1

- 인터파크 / YES24 크롤링 구현
- 크롤링 단위 테스트 작성

---

## 6/30(월) ~ 7/6(일) — 크롤링 구현 2

- 티켓링크 / 멜론티켓 크롤링 구현 (멜론 봇차단 graceful skip 포함)
- `VenueLayout` 모델 + `venue_layout_service.py` + 조회 엔드포인트 + 라우터 등록
- 크롤링 통합 단위 테스트 작성

---

## 7/7(월) ~ 7/13(일) — 크롤링 백엔드 연동

- `create_ticket` 백그라운드 태스크에 크롤링 트리거 연동
- 크롤링 결과 -> `TimeTable` / `Concert.price` / `VenueLayout` 자동 저장
- 통합 테스트 작성

---

## 7/14(월) ~ 7/20(일) — LLM 기능

### 취향 기반 추천 칼럼
- `Recommendation` 모델 + 마이그레이션
- `recommendation_service.py` — 유저 취향 집계 -> LLM -> 칼럼 생성
- 테스트 작성

### 공연 일기 생성
- `Ticket` 모델에 `diary` 필드 추가 (TEXT) + 마이그레이션
- `diary_service.py` — review 텍스트 + 공연 정보 -> LLM -> 일기 텍스트 저장
- 테스트 작성

---

## 7/21(월) ~ 7/27(일) — 백엔드 전체 점검

- API 응답 형식 및 에러 응답 표준화
- CORS 설정 + 인증/보안 점검
- 전체 테스트 재실행 + 성능 점검

---

## 7/28(월) ~ 8/3(일) — AWS 서버 연결

- EC2/ECS + RDS(PostgreSQL) + S3 버킷 설정
- Docker 이미지 빌드 + ECR 등록 + 배포
- 도메인 + HTTPS 설정 (Route53, ACM) (필수 X)
- ALB (Application Load Balancer) 설정
- 환경변수 관리 (Secrets Manager)

---

## 8/4(월) ~ 8/10(일) — 프론트-백엔드 연동

- 프론트-백엔드 연동

---

## 8/11(월) ~ 8/17(일) — 신규 기능

- 신규 기능 구현
- 프론트 / LLM 요청 사항 수정

---

## 8/18(월) ~ 8/24(일) — QA + 전체 점검

- E2E 시나리오 테스트 (전체 사용자 플로우)
- 성능 최적화
- 보안 점검
- 발견된 버그 수정

---

## 8/25(월) ~ 8/31(일) — 배포 후 마무리

- CloudWatch 모니터링 설정 (에러율, 응답시간, DB)
- 실서버 E2E 검증 + 핫픽스
- 최종 정리